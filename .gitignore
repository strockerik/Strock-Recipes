# Local secrets / environment
.env.local
notes.md

# Claude Code per-machine settings (local permission allowlist, not for the repo)
.claude/settings.local.json

# Personal recipe test photos — local calibration set, not for the public site
data/Recipe Photos/

# OS / editor
.DS_Store

# Python bytecode cache from one-off scripts/
__pycache__/
*.pyc

# Local recipe backups written by scripts/backup_recipes.py
backups/
The House Index — Features Guide.pdf

# Live AI test result archives (recipes only, no secrets) — kept for run-to-run comparison
ai_test_runs/
